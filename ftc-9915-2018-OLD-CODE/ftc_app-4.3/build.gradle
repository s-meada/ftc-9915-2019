/**
 * Top-level build file for ftc_app project.
 *
 * It is extraordinarily rare that you will ever need to edit this file.
 */
//you will not have to memorize how to write the robot code
//I have to take notes
//I need to have the conceps on the robot code
//The class extends OpMode indicating that it is an Opmode()
//Opmode is another xlass in the robot SDK
//Opmode includes init() and loop()
//Loop is where the actions happens
//
buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.4.0'
    }
}

// This is now required because aapt2 has to be downloaded from the
// google() repository beginning with version 3.2 of the Android Gradle Plugin
allprojects {
    repositories {
        google()
        jcenter()
    }
}
